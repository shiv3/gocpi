openapi: 3.1.0
components:
  parameters:
    TariffIdParameter:
      in: path
      name: tariff_id
      description: Tariff.id of the Tariff object
      required: true
      schema:
        type: string
        maxLength: 36
